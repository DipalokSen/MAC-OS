import React, { useRef } from 'react'

const Docker = () => {
  const dockerRef=useRef(null);
    return (
    <section id="docker">
    <div className='dock-container' ref={dockerRef}>

      



    </div>
      
    </section>
  )
}

export default Docker
